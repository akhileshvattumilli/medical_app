# Respiratory Resource Batch

Raw files copied from:

- `/Users/shrutwik/Downloads/resources`

Current status:

- Source files are archived in `source/`
- Batch metadata is set
- Template placeholder source mappings have been cleared
- `source_catalog.json` lists the files that still need segregation into app-ready items

Recommended next step:

1. Review each file in `source/`
2. Convert the material into case-linked records in `source_items.json`
3. Add or adjust systems, conditions, cases, and case details in `working/`
4. Run `npm run import:content -- imports/respiratory-resource-batch`
